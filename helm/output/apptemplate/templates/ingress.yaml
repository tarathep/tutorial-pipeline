---
# Source: apptemplate/templates/ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: <app-name>-ingress
  namespace: prd
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
    - host: tutorial.<your-dns-zone-name>
      http:
        paths:
          - backend:
              serviceName: <app-name>-service
              servicePort: http
            path: /
